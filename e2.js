const b = ["hola", "mundo", "desde", "vscode"];
   
// mostrar por pantalla: "hola - mundo, desde vscode."
function impresionSaludo(){
    for (let index = 0; index < b.length; index++) {
        var element = b[index] + " ";
        
        console.log(element)
        
        
    }
}

impresionSaludo()
